# Function-Plotter-Using-Java-GUI
This Function plotter uses alot of libraries(JFreeChart, Swing, awt, etc..).
This plotter supports (+ - * /) .
It can check the input and shows the wrong input if there is any.
